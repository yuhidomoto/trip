<% if mytrip.favorited_by?(current_user) %>
    <!-- ↑ もし、いいねを押していたら ↓ いいねをdeleteする -->
    <!-- ↓『link_to" "』とボタンの指定がない。この書き方は do〜end までがボタンになる。 -->
		<%= link_to mytrip_favorites_path(mytrip.id), method: :delete, remote: true do %>
				<i class="fa fa-heart" aria-hidden="true" style="color: red;"></i>
		<% end %>
    いいね　<%= mytrip.favorites.count %>
<% else %>
		<%= link_to mytrip_favorites_path(mytrip.id), method: :post,remote: true do %>
				<i class="far fa-heart"></i>
		<% end %>
    いいね　<%= mytrip.favorites.count %>
<% end %>